# 旅游路线推荐系统设计文档 v2

更新时间：2026-05-16

## 1. 文档定位

本文档描述 `tourism_route_demo` 当前代码库的真实运行方式、系统结构与核心算法实现。

- 以当前代码为准，不再以早期 CLI 交互版本为准。
- 当前系统是以 Web 为主的 simple-cycle 路线推荐系统。
- 当前推荐对象是闭环 `simple cycle` 路线，不再以旧的 `loop / out_and_back / repetitive_loop` 分类为主。

## 2. 系统概览

当前系统由五层组成：

1. Windows 启动包装层：`start.bat`
2. Python 启动层：`main.py`
3. Web 接入层：`web_api.py` + `frontend/`
4. 服务编排层：`web_service.py`
5. 算法与数据层：数据加载、季节推断、GBA 挖掘、路线生成、过滤、评分与 Top-K 选择

系统目标是：

- 从历史轨迹中挖掘高强度 simple cycle 模式
- 生成可解释的旅游路线候选集
- 结合用户个性化条件进行硬筛选与软排序
- 通过浏览器界面完成数据导入、推荐请求和结果查看

## 3. 当前运行方式

### 3.1 推荐用户入口：`start.bat`

当前面向 Windows 用户的推荐启动方式是双击项目根目录下的：

```text
start.bat
```

`start.bat` 当前会自动执行：

1. 创建虚拟环境 `.venv`
2. 激活虚拟环境
3. 升级 `pip`
4. 安装 `requirements.txt` 中的依赖
5. 调用 `main.py`

脚本优先尝试：

```text
py -3 -m venv .venv
```

如果失败，则回退为：

```text
python -m venv .venv
```

### 3.2 Python 启动入口：`main.py`

`main.py` 仍然是系统真正的 Python 启动器，当前职责只有：

1. 启动 `uvicorn`
2. 托管 `web_api:app`
3. 等待服务就绪
4. 自动打开默认浏览器

当前固定运行参数为：

- `HOST = "127.0.0.1"`
- `PORT = 8000`
- `reload = False`

默认访问地址为：

```text
http://127.0.0.1:8000/
```

### 3.3 手动运行方式

如果不使用 `start.bat`，当前最稳妥的手动运行方式是：

```powershell
python -m venv .venv
.\.venv\Scripts\python.exe -m pip install --upgrade pip
.\.venv\Scripts\python.exe -m pip install -r requirements.txt
.\.venv\Scripts\python.exe main.py
```

### 3.4 解释器不一致问题

当前系统的常见运行问题不是 `main.py` 逻辑错误，而是解释器不一致：

- `start.bat` 创建的 `.venv` 只对该次启动有效
- 它不会永久修改 PowerShell 的默认解释器
- 它也不会自动切换 PyCharm / VS Code 的项目解释器

因此，运行过 `start.bat` 之后，如果直接在 IDE 中运行 `main.py`，仍可能因为没有使用 `.venv` 而报缺包错误。

## 4. 前后端组织方式

### 4.1 前端形态

当前前端不是独立的 `npm` 工程，而是由 FastAPI 托管的静态页面：

- `frontend/index.html`
- `frontend/app.js`
- `frontend/styles.css`

前端页面通过 `GET /` 返回，静态资源通过 `/static` 挂载。

### 4.2 前端当前职责

前端负责：

- 收集推荐输入
- 上传 `poi.csv` 和 `trajectory.csv`
- 调用推荐接口
- 展示推荐卡片、详情区域和系统日志

当前浏览器可见界面的主要模块包括：

- Recommendation Input
- Data Import
- System Log
- Recommendation Results
- Route Details

### 4.3 前端初始化请求

前端初始化时会请求：

- `GET /api/categories`
- `GET /api/pois`
- `GET /api/stats`

提交推荐时请求：

- `POST /api/recommend`

导入数据时请求：

- `POST /api/data/import`

## 5. Web 接口层设计

`web_api.py` 是当前系统唯一的 HTTP 接入层。

### 5.1 已暴露接口

- `GET /`
- `GET /api/categories`
- `GET /api/pois`
- `GET /api/stats`
- `POST /api/recommend`
- `POST /api/data/import`

### 5.2 启动期行为

应用启动时会尝试执行 `RecommendationService.initialize()`：

- 如果 `data/poi.csv` 和 `data/trajectory.csv` 存在，则直接加载系统数据
- 如果默认数据不存在，则服务仍然启动，允许用户先进入 Web 页面后再导入数据

这保证了系统即使在“无默认数据”的情况下，也可以先打开前端页面。

## 6. 服务编排层设计

`web_service.py` 是当前系统的主编排层，负责：

- 读取配置
- 初始化缓存状态
- 校验与归一化请求
- 串联完整推荐流程
- 构造前端可直接消费的响应结构

### 6.1 内存缓存状态

`SystemState` 当前缓存：

- `poi_dict`
- `trajectory_dict`
- `trajectory_seasons`
- `sequences`
- `circular_patterns`
- `pair_tpi`
- `graph_node_count`
- `graph_edge_count`
- `poi_popularity`

### 6.2 当前推荐主链路

当前 Web 主链路为：

1. `generate_routes_from_patterns(...)`
2. `filter_routes_by_personalization(...)`
3. `filter_routes(...)`
4. `apply_personalized_hard_filters(...)`
5. `score_routes_for_recommendation(...)`
6. `top_k_recommendations(...)`

这就是当前真实运行链路，不再以旧的 CLI 命名流程为主。

## 7. 数据层设计

### 7.1 默认运行数据

当前系统默认读取：

```text
data/poi.csv
data/trajectory.csv
```

### 7.2 独立测试数据

当前仓库额外提供了一套演示和测试用数据集：

```text
mock_dataset/poi.csv
mock_dataset/trajectory.csv
```

这套数据的定位是：

- 用于让其他人在 GitHub 拉取项目后快速测试系统
- 不覆盖默认 `data/` 目录
- 可直接通过前端 `Data Import` 功能导入

当前这套 `mock_dataset` 具备以下特征：

- 16 个 POI
- 24 条 trajectory
- 可挖掘出多条 simple-cycle 闭环模式

### 7.3 `poi.csv` 字段要求

必需字段：

- `poi_id`
- `poi_name`
- `category`
- `latitude`
- `longitude`

可缺省字段及默认补全：

- `visit_duration_minutes`：缺失时默认 `60.0`
- `opening_hours`：缺失时默认 `"09:00-18:00"`

### 7.4 `trajectory.csv` 字段要求

必需字段：

- `user_id`
- `trajectory_id`
- `visit_order`
- `poi_id`
- `timestamp`

### 7.5 数据加载

`data_loader.py` 负责：

- 读取 POI 数据
- 读取轨迹数据
- 将其转换为下游模块使用的字典结构

### 7.6 轨迹序列构造

`trajectory_processor.py` 的 `build_ordered_sequences(...)` 负责按 `visit_order` 构造每条轨迹的 POI 序列。

当前默认配置下：

- `collapse_consecutive_duplicates = True`

即相邻重复 POI 会被压缩。

## 8. 季节性处理设计

`seasonality.py` 负责：

- 从轨迹时间戳推断轨迹季节
- 汇总 POI 的适宜季节
- 构造路线级季节明细

当前初始化阶段会执行：

1. `build_trajectory_season_lookup(...)`
2. `build_poi_suitable_season_lookup(...)`
3. `attach_poi_suitable_seasons(...)`

请求中的季节字段会通过 `normalize_requested_season(...)` 先做标准化。

## 9. GBA 挖掘设计

### 9.1 挖掘目标

当前挖掘层只保留闭环 simple cycle 模式：

- 首尾 POI 闭合
- 中间核心 POI 不重复
- 后续路线去重依赖 `canonical_cycle_signature`

### 9.2 挖掘主模块

- `pattern_mining.py`
- `graph_builder.py`
- `gba_algorithm.py`

### 9.3 核心过程

当前挖掘流程可概括为：

1. 从轨迹序列中统计 2-size 有向 pair
2. 计算 `pair_tpi`
3. 保留满足 `min_prev` 的 prevalent pair
4. 基于 prevalent pair 构建有向图
5. 在图中搜索 simple cycle
6. 为每个 cycle 计算 `cycle_support`
7. 计算 `cycle_tpi`
8. 计算 `cycle_strength_global`

### 9.4 当前模式字段

当前 simple-cycle 路线语义以以下字段为主：

- `is_closed_loop = True`
- `is_simple_cycle = True`
- `cycle_signature`
- `cycle_support`
- `cycle_tpi`
- `cycle_strength_global`
- `avg_pair_tpi`

旧结构字段已经不属于当前主路径：

- `route_structure_type`
- `out_and_back_score`
- `repetitive_loop_score`
- `repeat_ratio`
- `structure_quality_score`

## 10. 路线生成与候选表示

`route_generator.py` 负责将挖掘得到的闭环模式转换为推荐阶段使用的候选路线。

候选路线的核心语义包括：

- `poi_route`
- `categories`
- `path_length`
- `unique_poi_count`
- `canonical_cycle_signature`
- `cycle_support`
- `cycle_tpi`
- `cycle_strength_global`
- `avg_pair_tpi`

后续个性化阶段还会补充：

- `must_include_match_count`
- `must_include_total_count`
- `must_visit_fit_score`
- `start_distance_km`
- `start_fit_score`
- `season_fit_score`
- `dominant_supporting_season`

## 11. 请求模型与个性化设计

### 11.1 用户请求模型

当前统一请求模型为 `models.py` 中的 `UserRequest`：

- `preferred_categories: list[str]`
- `requested_season: str | None`
- `start_poi: str | None`
- `must_include_pois: list[str]`
- `time_budget_hours: float | None`
- `hard_filter_keys: list[str]`
- `soft_rank_groups: list[list[str]]`
- `soft_rank_weights: dict[str, float]`

### 11.2 个性化条件键

当前系统的标准个性化键为：

- `time_budget`
- `must_visit`
- `start_point`
- `type_preference`
- `season`

其中：

- `HARD_FILTER_KEYS` 只允许 `time_budget / must_visit / start_point / season`
- `SOFT_RANK_KEYS` 允许 `time_budget / must_visit / start_point / type_preference / season`

### 11.3 请求校验规则

`web_service.py` 当前会执行：

- 输入去重并保序
- 检查类别是否存在
- 检查起点 POI 是否存在
- 检查必去 POI 是否存在
- 检查 `time_budget_hours > 0`
- 检查 `top_k > 0`
- 检查 `hard_filter_keys` 是否合法
- 检查 `hard_filter_keys` 是否都有对应输入值
- 检查 `soft_rank_groups` 中是否存在空组
- 检查 `soft_rank_groups` 条件是否合法
- 检查 `soft_rank_groups` 是否与 `hard_filter_keys` 冲突
- 检查 `soft_rank_groups` 是否重复使用同一条件
- 检查 `soft_rank_groups` 是否都有对应输入值

### 11.4 软排序权重生成

`soft_rank_weights` 由 `soft_rank_groups` 自动生成，不是前端直接提交的最终权重。

当前默认优先级分数表：

- 第 1 组：`1.0`
- 第 2 组：`0.7`
- 第 3 组：`0.4`
- 第 4 组：`0.2`

随后归一化为和为 1 的权重字典。

### 11.5 个性化设置对象

当前主链路使用：

- `PersonalizationSettings`

其内部包含：

- `hard_filter_keys`
- `soft_rank_groups`
- `soft_rank_weights`

## 12. 过滤设计

### 12.1 个性化预过滤

`filter_routes_by_personalization(...)` 会先补充路线上的个性化信号，并在必要时输出回退说明，例如：

- 无法完全覆盖必去点时按覆盖度回退
- 起点不在强闭环中时按起点距离回退

### 12.2 通用质量过滤

`filter_routes(...)` 当前会过滤：

- 路线长度不在 `[min_route_pois, max_route_pois]` 范围内
- `cycle_support < min_cycle_support`
- `cycle_tpi < min_prev`
- 总距离超过 `max_route_distance_km`
- 如果传入时间预算，预计时长超过 `hard_time_overrun_ratio`

返回的移除统计包括：

- `removed_by_time`
- `removed_by_distance`
- `removed_by_cycle_tpi`
- `removed_by_length`

### 12.3 个性化硬筛选

`apply_personalized_hard_filters(...)` 是当前主路径的个性化硬筛选函数。

固定检查顺序为：

1. `time_budget`
2. `must_visit`
3. `start_point`
4. `season`

当前规则：

- `time_budget`：预计总时长必须小于等于用户预算
- `must_visit`：路线必须覆盖全部必去点
- `start_point`：`start_distance_km <= hard_start_distance_threshold_km`
- `season`：`dominant_supporting_season == requested_season`

返回移除统计包括：

- `removed_by_time_budget`
- `removed_by_must_visit`
- `removed_by_start_point`
- `removed_by_season`

## 13. 评分设计

`route_scoring.py` 当前保留两套评分接口：

- `score_routes(...)`
- `score_routes_for_recommendation(...)`

当前 Web 主路径实际使用的是：

- `score_routes_for_recommendation(...)`

### 13.1 主路径评分结构

主路径评分由三部分组成：

1. `base_score`
2. `personal_score`
3. `route_length_score`

#### 13.1.1 基础分

```text
base_score = 0.5 * route.cycle_tpi + 0.5 * route.cycle_strength_global
```

#### 13.1.2 个性化软排序分

时间预算分：

```text
time_score = max(
    0.0,
    1.0 - abs(estimated_hours - request.time_budget_hours) / request.time_budget_hours
)
```

必去点分：

```text
must_score = route.must_visit_fit_score
```

起点分：

```text
start_score = route.start_fit_score
```

季节分：

```text
season_score = route.season_fit_score
```

类别偏好分来自 `_preference_soft_rank_scores(...)`：

```text
preference_score = 0.6 * coverage + 0.4 * balance
```

个性化总分：

```text
personal_score = sum(settings.soft_rank_weights[key] * score for key, score in soft_scores.items())
```

#### 13.1.3 最终分

```text
final_score =
    config.base_score_weight * base_score
    + config.personal_score_weight * personal_score
    + config.route_length_reward_weight * route_length_score
```

### 13.2 当前默认权重

- `base_score_weight = 0.4`
- `personal_score_weight = 0.6`
- `route_length_reward_weight = 0.12`

### 13.3 评分输出对象

`ScoredRoute` 当前保留：

- `score`
- `base_score`
- `personal_score`
- `cycle_tpi`
- `cycle_strength_global`
- `popularity_score`
- `preference_score`
- `must_visit_fit_score`
- `start_fit_score`
- `time_fit_score`
- `season_fit_score`
- `estimated_total_duration`
- `travel_cost_km`
- `reason_tags`
- `fallback_reason`

## 14. Top-K 选择与去重设计

`recommendation.py` 中的 `top_k_recommendations(...)` 负责最终结果选择。

### 14.1 Top-K 上限

当前系统使用：

```text
effective_top_k = min(requested_top_k, config.top_k)
```

默认：

- `config.top_k = 10`

### 14.2 去重逻辑

当前去重基于：

- `canonical_cycle_signature`

同一闭环签名只保留一条。

### 14.3 多样性逻辑

当前采取“两阶段选择”：

1. 第一轮按相似度阈值执行严格多样化选择
2. 若数量不足，再放宽重叠限制，但仍保持签名去重

默认多样性阈值：

- `topk_similarity_threshold = 0.30`

## 15. Web 请求与响应契约

### 15.1 `POST /api/recommend` 请求体

当前请求字段为：

```json
{
  "preferred_categories": [],
  "season": null,
  "start_poi_id": null,
  "must_visit_poi_ids": [],
  "time_budget_hours": null,
  "top_k": null,
  "hard_filter_keys": [],
  "soft_rank_groups": []
}
```

### 15.2 顶层响应结构

当前固定返回：

```json
{
  "request": {},
  "meta": {},
  "routes": []
}
```

### 15.3 `meta` 关键字段

当前 `meta` 包含：

- `requested_top_k`
- `effective_top_k`
- `initial_candidate_count`
- `after_personalization_count`
- `after_filter_count`
- `after_web_hard_filter_count`
- `personalization_mode`
- `personalization_summary`
- `quality_filter_removed`
- `web_hard_filter_removed`
- `fallback_reason`

其中：

- `personalization_mode = "hard_filter_plus_soft_rank"`

### 15.4 `routes` 关键字段

当前每条路线至少包含：

- `rank`
- `score`
- `base_score`
- `personal_score`
- `hard_filter_passed`
- `soft_rank_weights`
- `personalization_summary`
- `route_poi_ids`
- `route_poi_names`
- `route_poi_details`
- `route_categories`
- `path_length`
- `unique_poi_count`
- `cycle_signature`
- `is_closed_loop`
- `is_simple_cycle`
- `display_route_summary`
- `pattern_score`
- `cycle_support`
- `cycle_tpi`
- `cycle_strength_global`
- `avg_tpi`
- `avg_pair_tpi`
- `popularity_score`
- `preference_match_score`
- `start_fit_score`
- `time_fit_score`
- `season_fit_score`
- `route_length_score`
- `dominant_supporting_season`
- `must_visit_fit_score`
- `travel_cost_km`
- `estimated_total_duration_minutes`
- `reason_tags`
- `fallback_reason`

## 16. 配置设计

核心配置定义在 `config.py` 的 `PipelineConfig` 中。

### 16.1 当前关键默认值

- `min_prev = 0.04`
- `max_cycle_length = 20`
- `min_gba_unique_pois = 2`
- `collapse_consecutive_duplicates = True`
- `seasonality_min_share_threshold = 0.20`
- `top_k = 10`
- `average_speed_kmh = 20.0`
- `max_route_distance_km = 1000.0`
- `hard_time_overrun_ratio = 1.5`
- `soft_time_overrun_ratio = 1.2`
- `min_cycle_support = 1`
- `min_route_pois = 2`
- `max_route_pois = 40`
- `preferred_route_poi_count = 4`
- `topk_similarity_threshold = 0.30`
- `route_length_reward_weight = 0.12`
- `hard_start_distance_threshold_km = 3.0`

## 17. 兼容层与历史遗留

为了不一次性打断历史模块，当前代码仍保留少量兼容别名，但它们不是当前第一方主流程的首选写法。

### 17.1 仍保留的兼容别名

- `apply_cli_hard_filters = apply_personalized_hard_filters`
- `CLIHardFilterResult = PersonalizedHardFilterResult`
- `score_routes_for_cli = score_routes_for_recommendation`
- `CLIPersonalizationSettings = PersonalizationSettings`
- `cli_base_score_weight -> base_score_weight`
- `cli_personal_score_weight -> personal_score_weight`

### 17.2 已移除的旧路径

以下旧路径已不再保留在当前仓库主运行形态中：

- 运行 `main.py` 后进入命令行交互
- 运行 `main.py` 自动导出统一 JSON

当前 `main.py` 已不再负责：

- `argparse`
- `input(...)`
- CLI 推荐打印
- 统一导出

## 18. 测试设计

### 18.1 当前自动化测试文件

当前核心测试包括：

- `tests/test_main_launcher.py`
- `tests/test_simple_cycle_smoke.py`
- `tests/test_windows_startup_contract.py`

### 18.2 当前验证重点

`tests/test_main_launcher.py` 主要验证：

- `main.run()` 是否启动浏览器线程
- 是否按固定 `host/port/reload` 调用 `uvicorn.run(...)`
- `main.py` 中是否已移除 CLI 入口调用
- `web_service.py` 是否使用当前中性命名主链路

`tests/test_simple_cycle_smoke.py` 主要验证：

- 核心模块是否可导入
- Web 推荐接口是否返回 simple-cycle 字段
- 响应中是否已移除旧结构字段
- 季节 tie-break 行为是否正确
- 新主链路命名下是否能正常产出推荐结果
- 当前 Web-only 运行形态不再要求 `cli_interface.py` 存在

`tests/test_windows_startup_contract.py` 主要验证：

- `start.bat` 是否存在并包含 `.venv + pip install + main.py` 启动链
- `README.md` 是否已切换到 Windows-focused 运行说明
- `requirements.txt` 是否包含当前直接依赖

## 19. 当前已知限制

### 19.1 系统当前偏向 Windows 交付

当前 README 和一键启动入口都以 Windows 为主。仓库当前没有提供：

- `start.sh`
- `start.ps1`
- 面向 macOS / Linux 的完整官方启动说明

### 19.2 Web 依赖解释器环境

如果当前 Python 环境未安装 `uvicorn`、`fastapi` 等依赖，`main.py` 无法启动。这是环境依赖问题，不属于推荐逻辑缺陷。

### 19.3 FastAPI 启动事件接口有弃用提示

当前 `web_api.py` 仍使用 `@app.on_event("startup")`。这会产生弃用警告，但当前不影响功能。

### 19.4 前端不是独立构建工程

当前前端没有独立的 `npm run dev` 工作流，它是由 FastAPI 托管的静态页面。

### 19.5 默认只监听本机

当前 `main.py` 监听：

```text
127.0.0.1:8000
```

因此当前默认访问方式是本机浏览器访问本机服务。如果希望局域网内其他电脑访问，需要单独修改 host 绑定方式。

## 20. 当前系统结论

截至本文档更新时间，`tourism_route_demo` 的当前形态可以概括为：

- 用户层推荐入口已经收敛为 `start.bat`
- Python 启动入口保持为 `main.py`
- 运行形态已经稳定为 Web 系统
- 推荐对象已经收敛为 `simple cycle` 闭环路线
- 主链路已经收敛为“个性化预处理 + 通用过滤 + 个性化硬筛选 + 软排序评分 + Top-K 去重多样化”
- 仓库中已提供独立的 `mock_dataset/` 供外部测试者快速导入验证

后续如果代码继续调整，应优先同步本文件中的以下内容：

- 启动入口与运行方式
- Web API 契约
- 推荐主链路函数名
- 个性化字段名
- 响应结构字段
- 测试入口
- 演示数据目录
